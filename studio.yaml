# User-facing feature selection. Secrets always live in an ignored .env file.
# IANA zone used for every displayed schedule time; timezone_label is the short
# suffix shown next to it (e.g. "18:30 MSK").
timezone: Europe/Moscow
timezone_label: MSK

# Text posting, video posting and analytics are unconditional: every Studio
# runs all three. What remains here is what genuinely differs per deployment.
modules:
  site: true
  youtube: false
  instagram: true

analytics:
  # The primary analytics card for this Studio. Maru uses "video".
  default_tab: posts

video:
  prepare_lead_minutes: 15
  reminder_minutes: 5
  retention_hours: 24
