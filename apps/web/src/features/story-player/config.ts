/* =============================================================================
 * КОНСТАНТЫ И КОНФИГИ ПЛЕЕРА
 * -----------------------------------------------------------------------------
 * Что здесь: giscus (комментарии), брейкпоинт «десктоп», тайминги перелистывания.
 * Новую константу («магическое число») — сюда с комментарием, не в компонент.
 * ========================================================================== */

/** giscus — комментарии через GitHub Discussions. IDs берутся на giscus.app
 * после выбора репозитория; при смене репо обнови здесь (одно место). */
export const giscusConfig = {
  repo: "alexgetmancom/alexgetman.com",
  repoId: "R_kgDOSJwPnQ",
  category: "Announcements",
  categoryId: "DIC_kwDOSJwPnc4C-S2f",
} as const;

/** Сколько показывается пост без видео до автоперехода (мс). */
export const storyIntervalMs = 8500;

/** Порог свайпа на мобильном (px) и «остывание» колеса мыши (мс). */
export const swipeThresholdPx = 55;
export const wheelCooldownMs = 140;
