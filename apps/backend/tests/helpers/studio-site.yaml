# Fixture for tests whose subject is a Studio that serves a public website: the
# site workers, the site delivery target, its data directory and its analytics
# period. The product default has the site off, and these tests are about the
# case where it is on, so they say so rather than inheriting it.
timezone: UTC
timezone_label: UTC

site_enabled: true

video:
  prepare_lead_minutes: 15
  reminder_minutes: 5
  retention_hours: 24
