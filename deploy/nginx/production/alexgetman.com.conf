map $uri $link_header {
    "/"              "</.well-known/api-catalog>; rel=\"api-catalog\", </llms.txt>; rel=\"service-doc\"";
    "/index.html"     "</.well-known/api-catalog>; rel=\"api-catalog\", </llms.txt>; rel=\"service-doc\"";
    "/ru/"           "</.well-known/api-catalog>; rel=\"api-catalog\", </llms.txt>; rel=\"service-doc\"";
    "/ru/index.html"  "</.well-known/api-catalog>; rel=\"api-catalog\", </llms.txt>; rel=\"service-doc\"";
    default          "";
}

server {
    listen 80;
    server_name alexgetman.com www.alexgetman.com;
    return 301 https://alexgetman.com$request_uri;
}

server {
    listen 127.0.0.1:81;
    server_name alexgetman.com www.alexgetman.com;

    root /home/deploy/ialexey-web;
    index index.html;

    include mime.types;
    types {
        text/markdown md;
    }
    charset utf-8;

    location ^~ /zoom/webhook {
        proxy_pass http://127.0.0.1:8799;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location ^~ /tg-feed/webhook {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location = /tg-feed/healthz {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location = /stats {
        access_log off;
        add_header X-Robots-Tag "noindex, nofollow" always;
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location = /stats/pageview {
        access_log off;
        add_header X-Robots-Tag "noindex, nofollow" always;
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location = /pipeline-status {
        include /etc/nginx/snippets/alexgetman-command-center.conf;
    }

    location = /command-center {
        include /etc/nginx/snippets/alexgetman-command-center.conf;
    }

    location = /api/pipeline-status {
        include /etc/nginx/snippets/alexgetman-command-center.conf;
    }

    location = /api/command-center {
        include /etc/nginx/snippets/alexgetman-command-center.conf;
    }

    location = /api/ops-dashboard {
        include /etc/nginx/snippets/alexgetman-command-center.conf;
    }

    location = /api/command-center/action {
        include /etc/nginx/snippets/alexgetman-command-center.conf;
    }

    location ^~ /api/ {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    include /etc/nginx/snippets/alexgetman-cache.conf;

    # Serve the canonical sitemap and robots policy dynamically. This bypasses
    # stale files left by the retired static site-sync process.
    location = /robots.txt {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location = /sitemap.xml {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location = /sitemap-0.xml {
        return 301 /sitemap.xml;
    }

    location = /sitemap-index.xml {
        return 301 /sitemap.xml;
    }

    # Slugs created before 2026-07-22 lost the breve in Cyrillic "й".
    # Keep existing external links valid while the corrected URL is canonical.
    location = /ru/52/у-ребрендинга-codex-один-главныи-бенефициар-и-это-не-openai/ {
        return 301 https://alexgetman.com/ru/52/у-ребрендинга-codex-один-главный-бенефициар-и-это-не-openai/$is_args$args;
    }

    location = /ru/53/grok-4-5-новыи-конкурент-gpt-и-claude/ {
        return 301 https://alexgetman.com/ru/53/grok-4-5-новый-конкурент-gpt-и-claude/$is_args$args;
    }

    location = /ru/54/claude-code-получил-встроенныи-браузер/ {
        return 301 https://alexgetman.com/ru/54/claude-code-получил-встроенный-браузер/$is_args$args;
    }

    location ~ ^/posts/(.*)$ {
        return 301 https://alexgetman.com/ru/posts/$1;
    }

    location ~* \.md$ {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location ~* \.well-known/(oauth-|openid-|api-catalog) {
        default_type application/json;
        charset utf-8;
        add_header Content-Type "application/json; charset=utf-8" always;
        try_files $uri =404;
    }

    location ^~ /maru-media/ {
        proxy_pass http://127.0.0.1:8789/;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location / {
        add_header Link $link_header always;

        if ($http_accept ~* "text/markdown") {
            rewrite ^/$ /index.md last;
            rewrite ^/ru/$ /ru/index.md last;
            rewrite ^/([0-9]+)/([^/]+)/?$ /$1/$2.md last;
            rewrite ^/ru/([0-9]+)/([^/]+)/?$ /ru/$1/$2.md last;
        }

        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }

    location @backend {
        proxy_pass http://127.0.0.1:8788;
        include /etc/nginx/snippets/alexgetman-proxy-headers.conf;
    }
}
